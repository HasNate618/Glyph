<Window x:Class="Glyph.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Glyph.App"
        mc:Ignorable="d"
        Title="Glyph" Height="260" Width="520">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="16,16,16,8"
            FontSize="18"
            FontWeight="SemiBold"
            Text="Glyph (prototype)" />

        <TextBlock
            Grid.Row="1"
            Margin="16,0,16,8"
            TextWrapping="Wrap"
            Text="Press Ctrl+Shift+NumPad * to open the overlay. Then type: r → c (Chrome) or r → n (Notepad). Press Esc to cancel." />

        <TextBlock
            Grid.Row="2"
            Margin="16,0,16,16"
            TextWrapping="Wrap"
            Foreground="Gray"
            Text="Status: running. This window can be minimized; the hook stays active while the app is running." />
    </Grid>
</Window>
