<Window x:Class="Glyph.App.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowActivated="False"
        Focusable="False"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        Title="Glyph Overlay">
    <!-- Single-layer rounded panel only; window background stays transparent -->
    <Border
        Padding="20,14,20,14"
        CornerRadius="10"
        Background="#BF0B0F12"  
        SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="18" ShadowDepth="0" Opacity="0.45" />
        </Border.Effect>

        <StackPanel MinWidth="300" MaxWidth="420">
            <TextBlock
                x:Name="SequenceText"
                Foreground="#E6E9EE"
                FontSize="14"
                FontWeight="SemiBold"
                FontFamily="Segoe UI Variable, Segoe UI, Arial"
                Margin="0,0,0,8"
                Text="Leader" />
            <ItemsControl x:Name="OptionsList" Margin="0,2,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DockPanel LastChildFill="True" Margin="0,6,0,6">
                            <Border Width="28" Height="28" CornerRadius="8" Background="#2E3338" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,0,8,0">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    FontWeight="Bold"
                                    FontSize="13"
                                    Foreground="#E6E9EE"
                                    FontFamily="Segoe UI Variable, Segoe UI, Arial"
                                    Text="{Binding Key}" />
                            </Border>
                            <TextBlock
                                VerticalAlignment="Center"
                                Foreground="#D7DADD"
                                FontSize="13"
                                FontFamily="Segoe UI Variable, Segoe UI, Arial"
                                Text="{Binding Description}" />
                        </DockPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</Window>
